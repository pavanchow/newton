[package]
name = "newton"
version = "0.1.0"
license = "MIT"
edition = "2021"
authors = ["Pavan Nallamothu"]
description = "A 2D physics engine in Rust, written from scratch."

[[bin]]
name = "newton"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
